running the command "python randomize.py" will generate xml files for heroes with random stats, abilities, talents, etc. and AI files

ai, modifiedAI, and randomized.xml should be moved into Base.StormData
